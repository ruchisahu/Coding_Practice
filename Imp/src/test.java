public class test {
    /*
    public static int countPrimes(int n) {
        if (n < 2) return 0;

        boolean[] isP = new boolean[n];
        isP[0] = false;
        isP[1] = false;

        for (int i = 2; i < n; i++){
            isP[i] = true;
        }

        for (int i = 2; i < Math.sqrt(n); i++){
            System.out.println("this" + Math.sqrt(n));
            System.out.println("this" + i);
            if(isP[i]){
                for(int j = 2; i*j < n; j++){
                    isP[i*j] = false;
                }
            }
        }

        int count = 0;
        for (boolean x : isP){
            if(x) count++;
        }

        return count;
    }
*/
    public static void main(String args[]){
        
        //System.out.println(countPrimes(10));
    }
}
